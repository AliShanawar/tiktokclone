import 'package:flutter/material.dart';

class DiscoverPgae extends StatelessWidget {
  const DiscoverPgae({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
